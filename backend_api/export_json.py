import sqlite3
import json
import os

def export_samples():
    print("Dữ liệu cache API hiện được lưu trữ trực tiếp dưới dạng các tệp JSON trong thư mục 'json_cache/' thay vì trong Database.")

if __name__ == "__main__":
    export_samples()
